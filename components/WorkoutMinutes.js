import { View, Text } from "react-native";
import React from "react";

const WorkoutMinutes = () => {
  return (
    <View>
      <Text>WorkoutMinutes</Text>
    </View>
  );
};

export default WorkoutMinutes;
